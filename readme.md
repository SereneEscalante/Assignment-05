1️⃣ What is the difference between var, let, and const?
= var : can be redeclared and reupdated.
let : can be updated but not redeclared.
const : cannot be updated or redeclared.

2️⃣ What is the spread operator (...)?
= The spread operator exapnds an array or object into individual elements.

3️⃣ What is the difference between map(), filter(), and forEach()?
= map() : transforms every element in an array.
filter() : selects elements based on a  condition.
forEach() : executes a function for each element.

4️⃣ What is an arrow function?
= Arrow Function are used for functions that inherits quality from parent.

5️⃣ What are template literals?
= Template literals(``) used to allow for multi-line strings.